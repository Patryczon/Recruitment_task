name: sofomo_task
description: "Patryk's recruitment task"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  auto_route_generator: ^9.3.1
  auto_route: ^9.3.0+1
  dio: ^5.8.0+1
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_bloc: ^9.0.0
  freezed: ^2.5.8
  freezed_annotation: ^2.4.4
  get_it: ^8.0.3
  golden_toolkit: ^0.15.0
  injectable: ^2.5.0
  intl: any
  url_launcher: ^6.3.1
  github_repository:
    path: ./packages/github_repository

  cupertino_icons: ^1.0.8

dev_dependencies:
  build_runner: ^2.4.15
  injectable_generator: ^2.7.0
  flutter_lints: ^5.0.0
  mockito: ^5.4.5
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  generate: true
